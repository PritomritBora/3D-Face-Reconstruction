# Core
numpy>=1.24.0
opencv-python>=4.7.0
pyyaml>=6.0

# 3D processing
open3d>=0.17.0
trimesh>=3.22.0
networkx>=2.8.0

# COLMAP binary must be installed separately (see README)
# Ubuntu: sudo apt install colmap
